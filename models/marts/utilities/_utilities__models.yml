version: 2

models:
  - name: electricity
    columns:
      - name: timestamp
        tests:
          - unique
          - not_null
      - name: date
      - name: half_hour
      - name: kwh
        tests:
          - not_null